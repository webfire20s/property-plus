THIS WILL HAVE DAY WISE ALL SQ QUERIES
DATE = 6 APRIL


CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    phone VARCHAR(15) UNIQUE,
    email VARCHAR(100),
    password VARCHAR(255),
    role ENUM('user','admin','staff') DEFAULT 'user',
    rera_number VARCHAR(100),
    gst_number VARCHAR(100),
    is_verified TINYINT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE memberships (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50),
    property_limit INT,
    price DECIMAL(10,2),
    duration_days INT
);

INSERT INTO memberships (name, property_limit, price, duration_days) VALUES
('Basic', 5, 999, 365),
('Silver', 15, 1999, 365),
('Gold', 30, 3999, 365),
('Platinum', 9999, 6999, 365);

CREATE TABLE user_memberships (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    membership_id INT,
    start_date DATE,
    expiry_date DATE,
    status ENUM('active','expired') DEFAULT 'active'
);

CREATE TABLE payments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    amount DECIMAL(10,2),
    type ENUM('registration','membership','custom'),
    txn_id VARCHAR(100),
    status ENUM('pending','success','failed'),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE properties (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    title VARCHAR(255),
    description TEXT,
    price DECIMAL(10,2),
    city VARCHAR(100),
    property_type VARCHAR(100),
    status ENUM('pending','approved','rejected') DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE property_images (
    id INT AUTO_INCREMENT PRIMARY KEY,
    property_id INT,
    image_path VARCHAR(255)
);

UPDATE properties SET status = 'approved';

CREATE TABLE access_requests (
    id INT AUTO_INCREMENT PRIMARY KEY,
    property_id INT,
    requester_id INT,
    status ENUM('pending','approved','rejected') DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

UPDATE properties SET status = 'approved';

ALTER TABLE payments ADD plan_id INT;


date = 7 april 

CREATE TABLE admins (
    id INT AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(100),
    password VARCHAR(255)
);

INSERT INTO admins (email, password)
VALUES ('admin@gmail.com', '123456');

ALTER TABLE users ADD status ENUM('active','blocked') DEFAULT 'active';

===============================================================================================================================================================

date = 8 april 


ALTER TABLE users 
ADD approval_status ENUM('pending','approved','rejected') DEFAULT 'pending';

ALTER TABLE users 
ADD business_name VARCHAR(255),
ADD state VARCHAR(100),
ADD district VARCHAR(100);

ALTER TABLE users 
MODIFY status ENUM('pending','active','blocked') DEFAULT 'pending';

CREATE TABLE contact_views (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    property_id INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE contact_requests (
    id INT AUTO_INCREMENT PRIMARY KEY,
    sender_id INT,
    property_id INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

ALTER TABLE contact_requests 
ADD status ENUM('pending','accepted','rejected') DEFAULT 'pending';


date = 9 april 

ALTER TABLE users 
ADD category ENUM('builder','broker','agent','freelancer') DEFAULT 'agent';

ALTER TABLE memberships 
ADD category VARCHAR(50),
ADD features TEXT;


date = 10 april 

ALTER TABLE properties 
ADD category VARCHAR(50) AFTER title,
ADD purpose ENUM('buy','sell','rent') AFTER category,
ADD area DECIMAL(10,2) AFTER price,
ADD area_unit ENUM('sqft','sqyd','acre') AFTER area;

UPDATE properties 
SET category = 'Flats', purpose = 'sell', area = 1000, area_unit='sqft'
WHERE category IS NULL;

ALTER TABLE properties 
ADD address TEXT,
ADD state VARCHAR(100),
ADD pincode VARCHAR(10),
ADD latitude DECIMAL(10,8),
ADD longitude DECIMAL(11,8),
ADD facing VARCHAR(20),
ADD floor VARCHAR(20),
ADD property_age VARCHAR(50),
ADD amenities TEXT,
ADD verification_status ENUM('pending','verified','rejected') DEFAULT 'pending',
ADD rejection_reason TEXT;

CREATE TABLE property_documents (
    id INT AUTO_INCREMENT PRIMARY KEY,
    property_id INT,
    document_type VARCHAR(50),
    file_path VARCHAR(255),
    status ENUM('pending','verified','rejected') DEFAULT 'pending',
    uploaded_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE property_media (
    id INT AUTO_INCREMENT PRIMARY KEY,
    property_id INT,
    type ENUM('image','video','floorplan'),
    file_path VARCHAR(255)
);

ALTER TABLE properties ADD COLUMN video VARCHAR(255) NULL;

================================================================================================================================================================================================================

date = 13 april (monday)

ALTER TABLE property_documents 
ADD rejection_reason TEXT NULL;

====================================================================================================================================================================================================================

date = 14 april (tuesday)

CREATE TABLE payment_screenshots (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    type VARCHAR(50),
    screenshot VARCHAR(255),
    status ENUM('pending','approved','rejected') DEFAULT 'pending',
    rejection_reason TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

ALTER TABLE payment_screenshots ADD COLUMN plan_id INT NULL;

ALTER TABLE users 
ADD membership_plan INT NULL,
ADD membership_status VARCHAR(20) DEFAULT 'inactive';

ALTER TABLE payment_screenshots ADD amount DECIMAL(10,2);

========================================================================================================================================================================================================================================

date = 16 april 


UPDATE memberships SET property_limit = 2 WHERE name = 'Listing';
UPDATE memberships SET property_limit = 5 WHERE name = 'Basic';
UPDATE memberships SET property_limit = 10 WHERE name = 'Silver';
UPDATE memberships SET property_limit = 20 WHERE name = 'Gold';
UPDATE memberships SET property_limit = 9999 WHERE name = 'Platinum';